defaults:
  - /model: flextok  # Use the FlexTok model configuration
  - /dataset: imageNet  # Use the ImageNet dataset configuration


output_path: "/BS/data_mani_compress/work/thesis/thesis/data/datasets/imagenet_reconstruction_losses_new/imagenet_densities_RF_0_unconditional.json"  # Path to save the estimated densities
conditional: true  # Whether to perform conditional density estimation
keep_k: 0
hutchinson_samples: 4  # Number of samples for the Hutchinson estimator

register_path: "/BS/data_mani_compress/work/thesis/thesis/data/datasets/imagnet_register_tokens/imagnet_train_register_tokens.npz"

start_batch_idx: 0  # Starting batch index for processing
end_batch_idx: 10000  # Ending batch index for processing

timesteps: 50  # Number of timesteps for the density estimation process

dataset:
  root: "/scratch/inf0/user/mparcham/ILSVRC2012/"  # Root directory of the ImageNet dataset
  batch_size: 8  # Batch size for data loading

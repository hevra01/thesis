defaults:
  - /dataset: imageNet  # Use the ImageNet dataset configuration


output_file: "data/datasets/imageNet_edge_ratios/train_imageNet_edge_ratios.json"

dataset:
  split: train
  batch_size: 32  
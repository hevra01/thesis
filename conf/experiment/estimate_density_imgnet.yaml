defaults:
  - /model: denseflow  # Use the DenseFlow model configuration
  - /dataset: imageNet  # Use the ImageNet dataset configuration


# this is the checkpoint for the density estimation model
checkpoint_path: '/BS/data_mani_compress/work/thesis/thesis/external/DenseFlow/denseflow/imn64/imagenet64/check/checkpoint.pt'
resize: [64, 64] # bc denseflow expects 64x64 images

output_path: "imgnet_densities.json"  # Path to save the estimated densities
defaults:
  - /model: flextok  # Use the FlexTok model configuration
  - /dataset: imageNet  # Use the ImageNet dataset configuration


output_path: "data/datasets/imagenet_density_val/densities.json"  # Path to save the estimated densities
keep_k: 0
hutchinson_samples: 1  # Number of samples for the Hutchinson estimator

register_path: "all_registers_imagenet.json"

dataset:
  split: "val"  # Use the validation split of the dataset
  batch_size: 1  # Batch size for data loading

  